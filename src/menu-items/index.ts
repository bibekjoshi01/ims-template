// project import
import modules from './modules';
import others from './others';
import pages from './pages';
import { MenuItems } from './types';

// ==============================|| MENU ITEMS ||============================== //

const menuItems: MenuItems = {
  items: [pages, modules, others]
};

export default menuItems;
